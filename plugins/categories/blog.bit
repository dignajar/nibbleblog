<?php

echo '<ul>';

foreach($categories as $category)
{
	// URL generator
	$href = Url::category($category['slug'], $settings['friendly_urls']);

	echo '<li class="category"><a href="'.$href.'">'.$category['name'].'</a></li>';
}

echo '</ul>';

?>