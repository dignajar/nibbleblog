<?php

	echo '<div class="pager">';
	echo '<div class="pages">';

		if($pager['show_newer'])
		{
			echo '<a href="'.$pager['href_newer'].'" class="button next">'.$_LANG['NEWER_POSTS'].'</a>';
		}

		echo '<a class="home" href="'.HTML_PATH_ROOT.'">Home</a>';

		if($pager['show_older'])
		{
			echo '<a href="'.$pager['href_older'].'" class="button previous">'.$_LANG['OLDER_POSTS'].'</a>';
		}

	echo '</div>';
	echo '</div>';

?>