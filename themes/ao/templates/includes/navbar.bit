<?php

if( !empty($theme['nav_links']) )
{
    echo '<nav id="navbar">';
        foreach($theme['nav_links'] as $link)
            echo '<div class="navbutton">'.$link['link'].$link['text'].'</a></div>';
    echo '</nav>';
}

?>
